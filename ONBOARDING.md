# 飞书 App 配置指南（一次性）

这是 Emmy 上手里**唯一无法自动化**的部分——需要你在飞书开放平台网页上手动建一个企业自建应用。
照着做大约 **10 分钟**，**每人只需做一次**。

> 选定模式：**每人各自建一个 app**（各自的 App ID）。
> 原因：飞书长连接是"集群模式"，同一个 app 的同一事件只会随机投给一个在线客户端——
> 多人共用一个 app 会互相抢消息、导致漏收。所以"人人都有自己的 Emmy"要求每人一个独立 app。

---

## 总览：要做 7 件事

```
1. 建应用            →  2. 开机器人能力      →  3. 申请权限
       ↓
4. 订阅消息事件      →  5. 选"长连接"订阅方式  →  6. 发布版本
       ↓
7. 把机器人拉进群
```

外加本机两条命令：`lark-cli config init` + `lark-cli auth login`。

---

## 步骤详解

### 1. 创建企业自建应用
飞书开放平台 → 开发者后台 → 创建企业自建应用 → 填名称（如"Emmy"）、头像、描述。
创建后在"凭证与基础信息"里能看到 **App ID** 和 **App Secret**（第 8 步要用，先留着）。

### 2. 开启机器人能力
左侧"应用能力" → "机器人" → 启用。配好机器人名称/头像（这就是群里 @ 的那个身份）。

### 3. 申请权限（Scope）
左侧"权限管理" → 申请以下权限：

| 权限 | 用途 |
|------|------|
| `im:message.p2p_msg:readonly` | 接收单聊消息 |
| `im:message.group_at_msg:readonly` | 接收**群里 @机器人** 的消息 |
| `im:message:send_as_bot` | 以机器人身份**回复消息** |

> 之后随 Emmy 能力扩展，可再加 `docx` / `bitable` / `task` / `wiki` / `calendar` 等对应权限。

### 4. 订阅消息事件
左侧"事件与回调" → "事件配置" → 添加事件：`im.message.receive_v1`（接收消息）。

### 5. 选择"使用长连接接收事件"（⚠️ 有个反直觉的坑）
"事件与回调" → "订阅方式" → 选 **"使用长连接接收事件"** → 保存。

> **关键时序**：保存时，你本机必须**已经有一条长连接在线**，否则保存会失败！
> 所以正确顺序是：
> ```bash
> # 先在终端把长连接跑起来（保持这个窗口开着）
> lark-cli event consume im.message.receive_v1 --as bot
> ```
> **然后**再回飞书后台点"保存"。保存成功后这个临时窗口可以 Ctrl-C 关掉，正式运行交给 `./start.sh`。

### 6. 创建版本并发布
左侧"版本管理与发布" → 创建版本 → 提交发布/上线。
**不发布，前面所有配置都不生效。** 企业内可能需要管理员审核通过。

### 7. 把机器人拉进群
在飞书里打开目标群 → 群设置 → 添加成员 → 搜你的机器人名称 → 拉进群。
**机器人不在群里，群成员 @它 不会触发任何事件。**

---

## 本机授权（两条命令）

```bash
# 填入第 1 步拿到的 App ID / App Secret（Secret 走 stdin 不留 shell 历史，存进 macOS Keychain）
lark-cli config init

# 扫码 / 设备流授权一次
lark-cli auth login
```

---

## 配完自检

```bash
lark-cli doctor
```

期望 `config / auth / connectivity` 各项 `pass`。然后回 [QUICKSTART.md](QUICKSTART.md) 第三步 `./start.sh`。

## 排查

| 现象 | 多半原因 |
|------|---------|
| 后台保存"长连接订阅方式"失败 | 没有先跑 `lark-cli event consume`（见步骤 5） |
| @机器人完全没反应 | 没发布版本 / 机器人不在群里 / 权限没申请全 |
| 只有单聊能用、群里不行 | 缺 `im:message.group_at_msg:readonly` 权限 |
| `auth` / 鉴权失败 | `--as` 没用 `bot`，或 `config init` 的 App Secret 填错 |

---

*管理员代建场景*：若由管理员统一建 app 并下发，则步骤 1–6 由管理员完成，使用者只需拿到 `App ID / App Secret` 后做"本机授权"两条命令 + 步骤 7 拉群。注意**一个 app 只能在一处 `consume`**，不要多人共用同一个 app 同时收同一事件。
