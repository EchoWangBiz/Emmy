# Emmy 产品需求文档（PRD）

> v1.0 / 2026-06-17 · 产品：Emmy — 飞书 AI 智能体
> 本文档只回答「做什么、为谁做、为什么、做到什么算合格」；「怎么实现」见后续《技术方案》。

---

## 一、背景与目标

内部同事希望在飞书里用一句自然语言就驱动 AI 完成日常协作操作（写文档、发通知、记表格、建任务），而不必逐个打开飞书功能手动操作。

**目标**：让一个普通同事在自己的 Mac 上 clone 项目、配好飞书应用，就能在飞书 @ 一个**专属于自己**的机器人，用自然语言完成高频协作操作。大脑复用本机已有的 Claude Code，**零 LLM 配置、零服务器**。

## 二、目标用户与使用场景

- **用户**：有 Claude 订阅、能在飞书开放平台建自建应用的内部同事（开发 / 运营 / PM）。
- **场景**：工作时间在工位、Mac 开着；在飞书群或单聊 @Emmy 下指令。
- **典型指令**："@Emmy 把今天的会议纪要整理成文档发到这个群"。

## 三、要解决的问题 / 核心价值

1. 省去手动操作飞书各功能的麻烦——自然语言一句话搞定。
2. 不用配 API Key、不用服务器、不用运维——复用每人**已有的** Claude Code + 飞书身份，数据只在本机。

## 四、功能需求（FR）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| FR1 | 接收飞书 @ 消息（群/单聊），识别是否需要 Emmy 处理 | P0 |
| FR2 | 调用本机 Claude Code 理解指令并自主执行 | P0 |
| FR3 | 通过 lark-cli 完成飞书操作（MVP 能力见下） | P0 |
| FR4 | 把结果回复到原会话 | P0 |
| FR5 | 同会话多轮对话记忆（续聊） | P1 |
| FR6 | 定时任务（如每日日报） | P2 |

**能力范围（MVP 先收敛，降低出错面）**：
- **MVP 4 个高频低危能力**：发消息、写/改文档、记多维表格(Bitable)、建任务。
- **后续**：知识库、日历、复杂 Bitable、跨系统联动。

## 五、非功能需求（NFR）—— 对应已识别的硬伤

| 编号 | 需求 | 对应硬伤 | 优先级 |
|------|------|---------|--------|
| NFR1 **安全** | 默认最小权限（只读 + 仅向当前会话回消息）；删除/群发/批量改等**高危操作默认禁止或需用户二次确认**；防 prompt injection（群消息/文档正文里的指令不当命令执行） | H3 | **P0** |
| NFR2 **响应体验** | 收到 @ 后**数秒内先回执"处理中"**，完成再发结果；接受复杂任务分钟级延迟 | H1 | P0 |
| NFR3 **在线可用** | Mac 在线时可用；**认证失效 / 长连接断开要主动告警**，不静默挂掉 | H2/H4/H6 | P1 |
| NFR4 **上手成本** | clone + 引导式 init + 飞书建 app 一次性完成，有傻瓜文档 | H7 | P1 |
| NFR5 **隐私** | 凭证、会话数据只存本机（Keychain / `~/.claude`），不入库、不上传 | — | P0 |

## 六、范围：MVP vs 后续

**MVP（第一个可用版本）**：
- 飞书 @ → 本机 Claude Code → lark-cli「发消息 / 写文档 / 记表格 / 建任务」→ 回执 + 结果
- 两段式响应、最小权限白名单 + 高危操作二次确认、单会话续聊、基础文档与 init 引导

**明确放到后续迭代**：定时任务、流式进展显示、能力扩展、常驻进程降延迟、关键定时任务的云端兜底。

## 七、约束与假设

- 平台 macOS（Apple Silicon / Intel）；**每人本机各自建飞书 app**（飞书长连接集群模式不允许多人共用一个 app）。
- 大脑 = 本机 Claude Code（headless `claude -p`）；飞书能力 = lark-cli；事件 = 本地长连接接收。
- 单人单实例，无多租户。用户有 Claude 订阅。

## 八、验收标准（MVP）

| 编号 | 标准 |
|------|------|
| A1 | 群里 @Emmy "发条消息说 X"，对应消息正确发出 |
| A2 | @Emmy "写个文档，标题 X 内容 Y"，文档创建成功并回链接 |
| A3 | 高危指令（如"删掉某文档"）**触发二次确认**，未确认不执行 |
| A4 | @ 后 **5 秒内**收到"处理中"回执 |
| A5 | 认证失效时有**明确告警/提示**，而非静默无响应 |
| A6 | 新人照文档 `clone + init + 建 app`，能在合理时间内跑通第一条指令 |

## 九、非目标（明确不做）

- ❌ 7×24 云端常驻（这是本地定位的取舍，不追求）
- ❌ 多用户共享同一实例 / 同一 app
- ❌ 自建或配置 LLM（就用本机 Claude Code）
- ❌ MVP 不追求低延迟（接受它是"异步慢助手"）

## 十、风险

沿用已识别的 7 个硬伤与优先级（详见 [计划书.md](计划书.md) 第十二节）。MVP 阶段最需正面应对的三个：**H3 安全**（最高）、**H1 延迟体验**、**H4 认证静默失效**。

---

*下一步：本 PRD 确认后 → 产出《技术方案》→ 拆 todoList → 写代码。*
