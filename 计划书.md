# Emmy — 飞书智能体计划书

> Emmy 是一个**本地分发式**飞书 AI 智能体：每位内部同事在自己的 Mac 上 `git clone`、跑一次 `./init.sh`，
> 各自建一个飞书自建应用、启动后即拥有"自己的"智能体。
> **大脑直接复用本机已安装的 Claude Code**（自带 ReAct agent loop / 工具调用 / 会话持久化），
> **飞书事件经 `lark-cli event consume` 长连接(WebSocket)本地接收**（无需公网回调）。
> Emmy 让 Claude Code 用 Bash 调 lark-cli 完成文档、消息、多维表格、任务、知识库、日历等操作。

---

## 〇、定位（v0.5）

| 维度 | v0.3（已废弃） | **v0.4/v0.5（本版）** |
|------|---------------|---------------------|
| 部署 | ECS 中心化常驻服务 | **每人本地 Mac，clone 即用** |
| 多人 | 单实例多人共享(多租户) | **每人一实例、一 app，各自独立身份** |
| 飞书入口 | 公网 Webhook 回调 | **lark-cli 长连接 `event consume`（无公网/端口）** |
| Web 框架 | FastAPI | **无（不再接 webhook）** |
| 守护 | systemd | **launchd 用户级 LaunchAgent** |
| 认证 | ECS 集中 token | **各自本机 Claude Code 登录态 / setup-token** |
| **上手方式** | 手册式手动配 | **`./init.sh` 一键体检+装环境+引导（v0.5 新增）** |

> v0.5 在 v0.4 基础上聚焦"**轻便上手**"：一键 `init` 自动安装环境工具 + README/QuickStart/ONBOARDING 文档体系。

---

## 一、整体架构

```
┌────────────────────────────────────────────────────────────────────┐
│                     同事的 Mac（每人一份，互相独立）                  │
│                                                                    │
│   飞书开放平台 ◀══ WebSocket 长连接 ══╗  (各自的自建 app)            │
│                                      ║                              │
│   ┌──────────────────────────────────╨──────────────────────────┐ │
│   │  lark-cli event bus daemon (本地, 按需自启, UDS IPC)          │ │
│   │  lark-cli event consume im.message.receive_v1 --as bot       │ │
│   │      │ NDJSON：每行一个事件(@Emmy 的消息)                     │ │
│   └──────┼───────────────────────────────────────────────────────┘ │
│          ▼  (subprocess stdout)                                    │
│   ┌──────────────────────────────────────────────────────────────┐ │
│   │  Emmy 监听进程 (run.py, Python asyncio)                       │ │
│   │   listener: 逐行读 → event_id 去重 → mentions 判定@Emmy       │ │
│   │      → 单消费者 asyncio.Queue (串行处理)                       │ │
│   │   scheduler: APScheduler 定时任务 → 投同一队列                │ │
│   │   health: heartbeat 预检 claude 认证 + 独立告警               │ │
│   └──────┬───────────────────────────────────────────────────────┘ │
│          ▼  core/claude_runner.py (subprocess)                     │
│   ┌──────────────────────────────────────────────────────────────┐ │
│   │  claude -p --output-format json --session-id/--resume <uuid> │ │
│   │    --append-system-prompt "$(cat prompts/emmy_system.md)"    │ │
│   │    --permission-mode dontAsk --allowedTools 'Bash(lark-cli:*)'│ │
│   │    --disallowedTools ... --model ... --strict-mcp-config     │ │
│   │  认证: 本机 Claude Code 登录态 / CLAUDE_CODE_OAUTH_TOKEN      │ │
│   └──────┬───────────────────────────────────────────────────────┘ │
│          ▼  Claude Code 内置 Agent Loop (天然 ReAct)               │
│          └─ 内置 Bash(白名单) → 直接调 lark-cli → 飞书 OpenAPI     │
└────────────────────────────────────────────────────────────────────┘
```

**核心流程**：lark-cli 长连接把 @Emmy 的消息以 NDJSON 流式吐到 stdout；Emmy 监听进程逐行读取、
按 `event_id` 去重、据 `mentions` 判定是否被 @，构造 prompt 投入**单消费者串行队列**；worker 调本机
`claude -p`，Claude Code 在内置 agent loop 中用 Bash 调 lark-cli 干活，最终 `lark-cli im` 回复。

---

## 二、Claude Code 作为 Agent 引擎（核心设计）

Emmy 不配置任何 LLM，直接驱动本机已安装的 Claude Code（本机实测 `2.1.128`）。

### 2.1 调用方式
`core/claude_runner.py` 以 `subprocess` 跑 `claude -p --output-format json`，解析返回 JSON
（`result` / `session_id` / `total_cost_usd` / `is_error`）。不引入 Python SDK。

> 本机实测标志：`-p`、`--output-format json/stream-json`、`--session-id`/`--resume`/`--fork-session`、
> `--append-system-prompt`（**仅接字符串，无 `-file` 主选项**）、`--permission-mode dontAsk`、
> `--allowedTools`/`--disallowedTools`、`--model`、`--strict-mcp-config`、`--max-budget-usd`。**CLI 无 `--max-turns`**。

### 2.2 认证（措辞按实测严谨标注）
- **默认（clone 即用）**：复用本机 Claude Code 日常登录态（零配置）。`doctor` 显示 token *"will auto-refresh on next user API call"*——有自动刷新路径。
- **长挂兜底**：`claude setup-token` 生成长期 token，注入 LaunchAgent 的 `EnvironmentVariables.CLAUDE_CODE_OAUTH_TOKEN`（**非 `--bare` 才读 OAuth/订阅 token**）。
- 事实边界：macOS 凭证存 **Keychain**（非 `~/.claude/.credentials.json`，那是 Linux 路径）；"1h/15h 过期"系外部推断、本机未实测，仅作风险。
- 运行时防线：`health.py` heartbeat 预检 `is_error` + 失效**独立告警**（Emmy 自身挂时无法飞书自我告警）。

### 2.3 会话记忆
`chat_id` 派生确定性 UUID 作 `--session-id`，后续 `--resume`；历史由 Claude Code 持久化在
`~/.claude/projects/<cwd-slug>/<uuid>.jsonl`（本机实测，**按 cwd 归属、仓库外**）。**所有调用固定同一 cwd**。

---

## 三、触发方式

| 方式 | 入口 | 说明 |
|------|------|------|
| 飞书机器人 | 群/单聊 @Emmy | `lark-cli event consume` 长连接收 `im.message.receive_v1` |
| 定时任务 | APScheduler（AsyncIOScheduler，嵌入主进程） | 投入同一串行队列，与实时消息共享同会话保护 |
| CLI 终端 | `emmy run "..."` | 本地手动触发/运维 |

---

## 四、技术选型

| 模块 | 选型 | 理由 |
|------|------|------|
| 语言 | Python 3.12（系统自带 3.9.6，`init` 负责装） | 薄壳监听 + 脚本任务 |
| 运行模型 | 单进程 asyncio + **单消费者串行队列** | 单人单机并发极低，串行天然防 session 写冲突 |
| Agent 引擎 | 本机 Claude Code（headless `claude -p`） | 免配 LLM、免写 agent loop |
| 飞书事件 | `lark-cli event consume`（长连接 NDJSON） | 本地收事件，无公网/端口/验签 |
| 飞书能力 | Claude Code 用 Bash 直调 lark-cli | npm 包 **`@larksuite/cli`**（本机 `1.0.55`），能力全覆盖 |
| 定时任务 | APScheduler（AsyncIOScheduler 嵌入） | 与监听同进程，共享队列与会话锁；不用 cron |
| 守护 | launchd 用户级 **LaunchAgent** | 能读 Keychain/OAuth（LaunchDaemon 在 root 读不到） |
| 安装/上手 | **`./init.sh`（brew + npm 体系）** | 自动体检+装环境，幂等、失败降级 |

---

## 五、Emmy 能力矩阵

Claude Code 通过 Bash 调 lark-cli 实现；可用子命令写入 `prompts/emmy_system.md`。

| 能力 | lark-cli 命令 | 示例指令 |
|------|-------------|---------|
| 文档写作/整理 | `lark-cli docs` / `markdown` | "帮我写本周技术周报" |
| 消息通知 | `lark-cli im message create` | "通知 XX 群：部署已完成" |
| 多维表格 | `lark-cli base record ...` | "把今天的数据记入 Bitable" |
| 项目任务 | `lark-cli task` | "创建任务：修复登录 bug，截止周五" |
| 知识库 | `lark-cli wiki` | "把这份文档归档到知识库" |
| 日历 | `lark-cli calendar` | "下周一下午 3 点约个会" |

---

## 六、项目结构

```
emmy/
├── init.sh                       # ★一键上手：体检 + 自动装环境 + 引导配置（v0.5 新增）
├── start.sh                      # 起/停 Emmy：装 LaunchAgent 或前台运行
├── run.py                        # 入口：asyncio 主循环（listener + 队列 + scheduler + health）
├── cli.py                        # `emmy run "..."` 本地触发/运维
│
├── core/
│   ├── listener.py               # subprocess 跑 event consume；读 NDJSON；event_id 去重；@判定；投队列
│   ├── claude_runner.py          # ★subprocess 调 claude -p；解析 json；认证健康检查
│   ├── session_store.py          # {feishu_chat_id → 确定性 UUID} 映射
│   ├── queue_worker.py           # 单消费者 asyncio.Queue 串行处理
│   ├── scheduler.py              # APScheduler(AsyncIOScheduler) 定时任务 → 同一队列
│   ├── health.py                 # heartbeat 预检 claude 认证 + 独立告警通道
│   └── config.py                 # 加载 emmy.yaml
│
├── prompts/
│   └── emmy_system.example.md    # 人设模板 + lark-cli 能力清单（cp 为 emmy_system.md 个性化）
│
├── tasks/
│   └── daily_report.py           # 预置定时任务
│
├── deploy/
│   └── com.org.emmy.plist.template  # LaunchAgent 模板（占位 __USER__/__PROJECT_DIR__/__PATH__）
│
├── emmy.yaml.example             # 配置模板：cwd、model、白名单、定时任务、去重窗口
├── .env.example                  # CLAUDE_CODE_OAUTH_TOKEN(可选)、告警通道等
├── .gitignore
├── requirements.txt              # apscheduler / python-dotenv（无 fastapi/litellm）
│
├── README.md                     # ★门面：Emmy 是什么/能干什么/架构亮点（v0.5 新增）
├── QUICKSTART.md                 # ★最短上手：三步 + 前置清单 + 验证（v0.5 新增）
├── ONBOARDING.md                 # ★飞书 app 建应用全流程（v0.5 新增）
└── 计划书.md
```

---

## 七、核心模块设计

### 7.1 listener（长连接事件接入）
```python
async def listen(queue):
    proc = await asyncio.create_subprocess_exec(
        "lark-cli", "event", "consume", "im.message.receive_v1", "--as", "bot",
        stdout=PIPE, stderr=PIPE)
    await wait_ready(proc.stderr)                 # 读 stderr 至 "[event] ready ..." 再读 stdout
    async for line in proc.stdout:                # NDJSON
        ev = json.loads(line)
        if not seen.add_if_new(ev["event_id"]):   # ★必须去重：飞书 3s ack 超时会重推
            continue
        if not mentions_emmy(ev):                 # 据 mentions 判定是否被 @
            continue
        await queue.put(build_task(ev))
    # consume 退出 → 进程内重启循环（或交给 launchd KeepAlive）
```

### 7.2 queue_worker（单消费者串行）
```python
# 单人单机全程串行——省掉 Semaphore/同会话锁；串行天然保证不并发写同一 session jsonl。
async def worker(queue):
    while True:
        task = await queue.get()
        uuid = session_store.uuid_for(task.chat_id)
        res = await claude_runner.run(task.prompt, uuid, resume=session_store.seen(task.chat_id))
        await lark_reply(task.chat_id, res.text)
```

### 7.3 claude_runner（调用 + 白名单 + 认证）
```python
cmd = ["claude", "-p", prompt, "--output-format", "json",
       ("--resume" if resume else "--session-id"), uuid,
       "--append-system-prompt", SYSTEM_PROMPT_TEXT,
       "--permission-mode", "dontAsk",
       "--allowedTools", "Bash(lark-cli:*)",
       "--disallowedTools", "Bash(rm:*)", "Bash(curl:*)", "Bash(ssh:*)",
       "--model", "claude-sonnet-4-6", "--strict-mcp-config"]
# cwd 固定；env 注入 CLAUDE_CODE_OAUTH_TOKEN（若用 setup-token）；不能加 --bare
```

### 7.4 health（无人值守认证守护）
heartbeat 定时 `claude -p "ok"` 预检 `is_error`；失效 → 独立告警（非飞书自身）；setup-token 临近到期提醒。

---

## 八、飞书 App 配置（每人自建，一次性）

> 选定模式：**每人各自建一个企业自建应用**（各自 app-id，事件不被集群模式瓜分）。详见 **ONBOARDING.md**。

```
1. 飞书开放平台 → 创建企业自建应用（拿 App ID / App Secret）
2. 「应用能力 > 机器人」开启机器人能力
3. 「权限管理」申请：
   - im:message.p2p_msg:readonly        （本机 event schema 确认：单聊接收最小必需）
   - im:message.group_at_msg:readonly   （据飞书官方文档：群里 @机器人 必需）
   - im:message:send_as_bot             （据飞书官方文档：机器人回复消息必需）
4. 「事件与回调 > 事件配置」添加：im.message.receive_v1
5. 「订阅方式」选「使用长连接接收事件」并保存
   ⚠️ 保存时本机须有活跃长连接 → 先跑 `lark-cli event consume im.message.receive_v1 --as bot` 再保存
6. 创建版本并「发布/上线」
7. 把机器人拉进目标群

本机授权：
  npm i -g @larksuite/cli        # 若未装（init.sh 会自动装）
  lark-cli config init           # 填 App ID / App Secret（存 macOS Keychain）
  lark-cli auth login            # Device Flow（或 lark-cli auth qrcode）
```

---

## 九、一键上手与文档体系（v0.5 核心）

### 9.1 三步上手

```
① git clone <repo> && cd emmy
② ./init.sh        # 体检 + 自动装环境 + 引导人工步骤 + 验证
③ ./start.sh       # 起 Emmy；然后飞书拉机器人进群、@它
```

### 9.2 `./init.sh` 设计

落地形态：项目根的 **shell 脚本 `init.sh`**（用户口中的"`/init`"），最直接、无额外依赖；可配 `make init` 别名。
与 `start.sh` 分工：**init 跑一次**（装环境+引导配置），**start 反复用**（起/停服务）。

```
init.sh 流程：
  [体检]   逐项 command -v / brew list / npm ls -g 检测：
           Homebrew · node · claude(claude-code cask) · lark-cli(@larksuite/cli) · python@3.12
           → 打印 ✓/✗ 清单
  [确认]   列出"将要安装什么" → 等用户确认 y/n（不静默改全局环境）
  [安装]   缺什么装什么（已装跳过，幂等）：
           - 缺 brew → 官方安装脚本
           - brew install --cask claude-code
           - brew install node@22
           - npm i -g @larksuite/cli
           - brew install python@3.12 → python3.12 -m venv .venv → pip install -r requirements.txt
           - （可选）npx skills add larksuite/cli  # 给 Claude Code 装 lark API 技能包，提升调用准确度
  [引导]   打印无法自动的人工步骤指引：
           - claude 登录 / claude setup-token
           - 建飞书 app（→ ONBOARDING.md）+ lark-cli config init + auth login
  [验证]   lark-cli doctor（config/auth/connectivity）+ claude -p "ok" 解析 is_error
  [失败]   任一步失败 → 打印可复制的手动修复命令，记录失败项汇总，不中断；修完重跑 init.sh（幂等）
```

跨机器健壮性：**用 `$(brew --prefix)` 自适应** Apple Silicon(`/opt/homebrew`) / Intel(`/usr/local`)；
brew/npm -g 无需 sudo；node v26 已存在不强制降级，仅提醒建议 LTS。

### 9.3 文档体系

| 文档 | 角色 | 大纲 |
|------|------|------|
| **README.md** | 门面 | 是什么/能干什么(能力矩阵)/架构亮点(一图)/环境要求/链接 |
| **QUICKSTART.md** | 最短上手 | 前置清单(可否自动) + 三步 + 验证 + 日常操作 + FAQ |
| **ONBOARDING.md** | 建 app 细节 | 7 步建应用 + 长连接订阅时序坑 + 本机授权 + 排查 |

> 诚实原则：建飞书 app 是约 10 分钟、不可省的一次性人工活，QuickStart 把它清楚标为"前置人工步骤"，
> 与"`./init.sh` 自动装环境"分开，不假装它也能一键。

---

## 十、本地部署与守护（Mac）

### 10.1 LaunchAgent（用户级，非 LaunchDaemon）
`~/Library/LaunchAgents/com.<org>.emmy.plist` 关键字段：

| 字段 | 值 | 原因 |
|------|----|----|
| `ProgramArguments` | **全绝对路径** `[.../python3, .../run.py]` | launchd 不继承登录 shell |
| `WorkingDirectory` | 项目目录（**钉死**） | 决定 session jsonl 归属 |
| `KeepAlive` | `{Crashed:true, SuccessfulExit:false}` | 崩溃才拉起 |
| `EnvironmentVariables.PATH` | **必含 `$(brew --prefix)/bin`** | ★最常见坑：否则找不到 claude/lark-cli/node |
| `EnvironmentVariables.CLAUDE_CODE_OAUTH_TOKEN` | 可选 | 后台长挂兜底认证 |
| `StandardOutPath`/`StandardErrorPath` | `~/Library/Logs/emmy/*.log` | 排障 |

加载：`launchctl bootstrap gui/$(id -u) <plist>`；卸载：`bootout`。`start.sh` 封装这些。

### 10.2 分发隔离（.gitignore）

| 入库共享 | 不入库（每人本机各自） |
|---------|---------------------|
| 代码、`init.sh`/`start.sh`、plist 模板、`*.example`、README/QUICKSTART/ONBOARDING | app 凭证(Keychain)、auth 授权态、claude 登录态/token、`prompts/emmy_system.md`、`emmy.yaml`/`.env`、session 数据、生成的 plist、`*.log` |

`.gitignore` 要点：`.env`、`emmy.yaml`、`prompts/emmy_system.md`、`*.log`、本地 `*.plist`、`*secret*`/`*token*`、`.venv/`、`__pycache__/`。

---

## 十一、开发阶段

### Phase 0 — 前置打通（每人一次性）
- [ ] `./init.sh` 体检+装环境跑通；按 ONBOARDING 走通建 app→权限→长连接订阅→发布→拉群
- [ ] 手动 `lark-cli event consume im.message.receive_v1 --as bot` 收到一条真实 @消息

### Phase 1 — 核心链路（约 1 周）
- [ ] `core/listener.py`（读 NDJSON + event_id 去重 + @判定） + `core/claude_runner.py`（调 claude -p + 白名单）
- [ ] `prompts/emmy_system.example.md`（人设 + lark-cli 能力清单）
- [ ] 跑通"@Emmy → claude -p → lark-cli im 回复"

### Phase 2 — 服务化（约 1 周）
- [ ] `session_store.py` + `--resume` 续聊（固定 cwd）；`queue_worker.py` 单消费者串行
- [ ] `scheduler.py` 定时任务；`cli.py` 本地触发；`health.py` heartbeat

### Phase 3 — 上手与分发硬化（3~4 天）
- [ ] **`init.sh`**（体检+自动装+引导+验证，幂等+失败降级） + **`start.sh`**（LaunchAgent）
- [ ] **README / QUICKSTART / ONBOARDING** 三文档定稿；`.gitignore` 隔离
- [ ] 认证失效独立告警；event_id 去重表持久化；session jsonl 轮转清理
- [ ] consume 子进程 supervisor 自愈 + launchd KeepAlive 双保险

---

## 十二、待定事项 / 风险

| # | 项 | 类别 | 严重度 | 应对 |
|---|----|------|--------|------|
| 1 | 每人自建 app 门槛（需公司允许建企业应用 + 审核 + 发布） | onboarding | 🔴 高 | ONBOARDING.md 傻瓜化；管理员代建为备选 |
| 2 | 长连接订阅"先起 consume 再后台保存"时序耦合 | 配置 | 🟠 中 | init/ONBOARDING 明确引导先拉起长连接再保存 |
| 3 | **event_id 去重必做**（飞书 3s ack 超时重推，claude 处理远超 3s） | 正确性 | 🔴 高 | 按 event_id 幂等；去重表持久化/容忍窗口 |
| 4 | 后台认证过期、无人值守失败无自愈 | 认证 | 🔴 高 | 复用登录态 + setup-token 兜底；heartbeat + 独立告警 |
| 5 | **`init.sh` 自动改用户全局环境的风险** | 安装 | 🟠 中高 | 安装前列清单征得同意；幂等；失败降级给手动命令 |
| 6 | Intel Mac / 未装 brew / node v26 冲突 | 安装 | 🟠 中 | `$(brew --prefix)` 自适应；缺 brew 先装；v26 仅提醒不强制降级 |
| 7 | claude 登录 + 飞书建 app **本质无法自动**，只能引导 | 体验 | 🟠 中 | 文档诚实标注为人工前置，勿宣称"全自动一键" |
| 8 | plist 不补 PATH → 找不到 claude/lark-cli/node | 部署 | 🟠 中高 | 模板写死 `EnvironmentVariables.PATH` 含 `$(brew --prefix)/bin` |
| 9 | Keychain 依赖图形登录态（锁屏/纯 ssh 读不到） | 运维 | 🟠 中 | 文档说明依赖图形登录；或 setup-token 注入 env |
| 10 | session jsonl 无限增长且含对话明文 | 隐私/运维 | 🟠 中 | 定时轮转清理；离职擦除流程 |
| 11 | 长连接断线重连/长挂稳定性文档未逐字保证 | 可靠性 | 🟠 中 | 进程内 supervisor + launchd KeepAlive 双保险；需实测 |
| 12 | 人设模板漂移（emmy_system.md 不入库，.example 更新难合并） | 维护 | 🟡 低 | 文档约定模板更新通知 |
| 13 | node v26 超官方测试 LTS | 环境 | 🟡 低 | init 建议 node 20/22 LTS |
| 14 | Emmy 人设 | 体验 | 🟡 低 | `prompts/emmy_system.md` 注入外甥女人设 |

---

*计划书版本：v0.5 / 2026-06-17 — 本地 Mac 分发式（每人自建 app + lark-cli 长连接 + 本机 Claude Code 当大脑 + Bash 调 lark-cli + launchd 守护 + `./init.sh` 一键上手 + README/QUICKSTART/ONBOARDING 文档体系）*
